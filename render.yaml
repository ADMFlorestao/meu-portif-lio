services:
  - type: web
    name: meu-portfolio
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm run start:render
    healthCheckPath: /
    autoDeployTrigger: commit
